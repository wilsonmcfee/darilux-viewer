{
  "name": "@darilux/viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Darilux Studio's 3D Gaussian splat viewer — a config-driven engine for interactive room captures (WebGPU with WebGL2 fallback, hero fly-ins, walk mode, touch controls), consumed by per-client sites.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "playcanvas": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
