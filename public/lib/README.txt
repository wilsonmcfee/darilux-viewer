NOT NEEDED — kept as a note.

Earlier this folder was going to hold WebGPU shader-compiler helpers
(glslang / twgsl). The current PlayCanvas engine renders Gaussian splats on
WebGPU using WGSL shaders directly, so those helpers are NOT required — the
SuperSplat starter passes none, and neither does src/device.ts.

You can safely ignore or delete this folder. It's left here only so the earlier
reference in the docs makes sense.
